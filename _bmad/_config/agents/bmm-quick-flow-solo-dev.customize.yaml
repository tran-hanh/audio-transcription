# Agent Customization
# Customize any section below - all are optional

# Override agent name
agent:
  metadata:
    name: ""

# Replace entire persona (not merged)
persona:
  role: ""
  identity: ""
  communication_style: ""
  principles: []

# Add custom critical actions (appended after standard config loading)
critical_actions:
  - "Even for quick changes: before considering done, add or update tests (unit/integration/e2e as relevant) and update README or other relevant docs per CONTRIBUTING.md."

# Add persistent memories for the agent
memories:
  - "Project: Long Audio Transcription Tool (long audio transcribing via Gemini, Vietnamese-optimized). Backend: Flask + src/ (transcribe, gemini_client, audio_processor, transcription_service). Frontend: React/TypeScript. Chunk length 1-30 min (default 12), language vi."
  - "Even for quick changes: follow clean code; add/update tests (unit/integration/e2e) per TDD; update README and relevant docs. See CONTRIBUTING.md."

# Add custom menu items (appended to base menu)
# Don't include * prefix or help/exit - auto-injected
menu: []
# Example:
# menu:
#   - trigger: my-workflow
#     workflow: "{project-root}/custom/my.yaml"
#     description: My custom workflow

# Add custom prompts (for action="#id" handlers)
prompts: []
# Example:
# prompts:
# - id: my-prompt
#   content: |
#     Prompt instructions here
